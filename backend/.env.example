NODE_ENV=development
PORT=3000
# FRONTEND_URL: The frontend URL(s) allowed to access the API (CORS)
# Supports multiple comma-separated URLs for custom domains
# Examples:
#   Single URL: FRONTEND_URL=https://myapp.onrender.com
#   Multiple URLs: FRONTEND_URL=https://myapp.onrender.com,https://mycustomdomain.com
FRONTEND_URL=http://localhost:5173

# STORAGE_PATH: Path to persistent storage directory for uploads and data
# In production, set this to a persistent volume mount path (e.g., /var/data)
# If not set, defaults to the backend directory (not persistent in containerized deployments)
# STORAGE_PATH=/var/data

# ========================================
# Cloudflare R2 Configuration
# Required for automatic PDF thumbnail generation
# ========================================

# Your Cloudflare account ID (found in dashboard URL)
R2_ACCOUNT_ID=your_account_id_here

# R2 API Token credentials (create in Cloudflare dashboard > R2 > Manage R2 API Tokens)
R2_ACCESS_KEY_ID=your_access_key_here
R2_SECRET_ACCESS_KEY=your_secret_key_here

# R2 bucket name
R2_BUCKET_NAME=your_bucket_name

# Public URL for your R2 bucket (enable public access in R2 settings)
# Example: https://pub-abc123.r2.dev
R2_PUBLIC_URL=https://pub-xxxxx.r2.dev
