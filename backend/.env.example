NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173
# STORAGE_PATH: Path to persistent storage directory for uploads and data
# In production, set this to a persistent volume mount path (e.g., /var/data)
# If not set, defaults to the backend directory (not persistent in containerized deployments)
# STORAGE_PATH=/var/data
